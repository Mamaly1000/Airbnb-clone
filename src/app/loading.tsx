import Loader from "@/components/ui/Loader";
import React from "react";

const loading = () => {
  return <Loader className="min-h-screen max-h-screen w-full bg-white dark:bg-neutral-800" />;
};

export default loading;
